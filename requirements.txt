# Core analysis stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7

# Classical time-series models (STL, MSTL, SARIMAX, ETS, ADF/KPSS, Ljung-Box)
statsmodels>=0.14

# Feature selection, Isolation Forest, preprocessing
scikit-learn>=1.3

# Gradient-boosted global forecaster
lightgbm>=4.0

# Deep learning (LSTM / GRU). Optional — the pipeline degrades gracefully
# without it via `--no-deep`.
torch>=2.0

# Development / notebooks
pytest>=7.4
pytest-cov>=4.1
ruff>=0.4
jupyter>=1.0
